{
  "name": "apero-onboarding",
  "version": "2.0.0",
  "description": "APERO HR Onboarding — template generator + checklist tracker (Vercel + Vercel Blob)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemailer": "^6.9.15"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
